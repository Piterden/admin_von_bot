const kickAction = require('./kick')
const passAction = require('./pass')
const replyAction = require('./reply')
const actionsAction = require('./actions')
const editSettingAction = require('./edit-setting')

module.exports = {
  kickAction,
  passAction,
  replyAction,
  actionsAction,
  editSettingAction,
}
